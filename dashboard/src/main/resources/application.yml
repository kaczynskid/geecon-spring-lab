info:
  component: "${project.name}"

server:
  port: 8989

management:
  port: 8990

turbine:
  clusterNameExpression: new String('default')
  appConfig: customer-service,merchant-service,payback-service
