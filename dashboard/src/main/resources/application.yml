info:
  component: "${project.name}"

server:
  port: 8989

management:
  port: 8990

turbine:
  aggregator:
    clusterConfig: GeeCON
  appConfig: customer-service,merchant-service,payback-service
  clusterNameExpression: metadata['cluster']
